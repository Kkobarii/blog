---
import ArchivePanel from "@components/ArchivePanel.svelte";
import I18nKey from "@i18n/i18nKey";
import { i18n } from "@i18n/translation";
import MainGridLayout from "@layouts/MainGridLayout.astro";
import { getSortedPosts } from "../../utils/content-utils";
import { getLangFromUrl } from "@utils/lang-utils";

const lang = getLangFromUrl(Astro.url);
const sortedPosts = await getSortedPosts(lang);
---

<MainGridLayout title={i18n(I18nKey.archive, lang)}>
    <ArchivePanel sortedPosts={sortedPosts} client:only="svelte"></ArchivePanel>
</MainGridLayout>
